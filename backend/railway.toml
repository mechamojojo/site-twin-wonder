[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && node -e \"console.log('node-ok')\" && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
