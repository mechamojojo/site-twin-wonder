[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && echo '[railway] starting node' && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
